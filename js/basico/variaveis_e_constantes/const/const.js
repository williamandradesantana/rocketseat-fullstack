/* 
    Uma constante cria uma variável cujo o valor é fixo
    (não pode ser alterado)
*/
const number = 42;
console.log(number);

// Irá gerar um erro por que o valor não pode ser alterado
number = 55;
console.log(number);
